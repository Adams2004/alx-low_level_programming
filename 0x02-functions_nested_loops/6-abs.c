#include "main.h"

/**
 * _abs - computes the absolute value of an integer.
 * @n: an integer
 * Return: -n if n < 0, 0 otherwise
 */
int _abs(int n)
{

	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
